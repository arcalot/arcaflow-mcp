## Scripts

This directory contains developer and CI helper scripts, including:

- `dev-setup.sh` to install git hooks
- `test-*.sh` for component tests
- `validate.sh` for lint and formatting checks
